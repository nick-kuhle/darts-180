# Security policy

Do **not** open public issues for a potential vulnerability, leaked clip, authentication bypass,
or unsafe camera-data handling problem. Until a company security inbox exists, report privately to
the repository owners and include reproduction steps, affected version, and impact.

This scaffold has no production authentication or storage configured. It must not be deployed with
its in-memory API store or placeholder identifiers.
